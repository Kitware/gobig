
export MESOS_WORK_DIR="{{ mesos_data_root }}/master"
export MESOS_EXTERNAL_LOG_FILE="/var/lib/mesos-master.log"
export MESOS_LOG_DIR="{{ mesos_prefix }}/logs"
export MESOS_LOGGING_LEVEL="INFO"
export MESOS_PORT="{{ mesos_master_port }}"
export MESOS_QUORUM="{{ mesos_quorum }}"
export MESOS_ZK="{{ mesos_zookeeper_url }}"
export MESOS_ALLOCATION_INTERVAL="{{ mesos_allocation_interval }}"
export MESOS_ALLOCATOR="{{ mesos_allocator }}"
export MESOS_CLUSTER="{{ mesos_cluster_name }}"
export MESOS_MAX_SLAVE_PING_TIMEOUTS="{{ mesos_max_slave_ping_timeouts }}"
export MESOS_SLAVE_PING_TIMEOUT="{{ mesos_slave_ping_timeout }}"
export MESOS_SLAVE_REREGISTER_TIMEOUT="{{ mesos_slave_reregister_timeout }}"
export MESOS_OFFER_TIMEOUT="{{ mesos_offer_timeout }}"
export MESOS_REGISTRY_FETCH_TIMEOUT="{{ mesos_registry_fetch_timeout }}"
export MESOS_REGISTRY_STORE_TIMEOUT="{{ mesos_registry_store_timeout }}"
export MESOS_WEBUI_DIR="{{ mesos_data_root }}/master/webui"
export MESOS_ZK_SESSION_TIMEOUT="{{ mesos_zookeeper_session_timeout }}"
{% if mesos_hadoop_home != "" %}
export MESOS_HADOOP_HOME="{{ mesos_hadoop_home }}"
export HADOOP_HOME="{{ mesos_hadoop_home }}"
{% endif %}

